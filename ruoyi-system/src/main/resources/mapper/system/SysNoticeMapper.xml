<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysNoticeMapper">
    
    <resultMap type="SysNotice" id="SysNoticeResult">
        <id     property="noticeId"     column="notice_id"    />
        <result property="noticeTitle"  column="notice_title" />
        <result property="noticeType"   column="notice_type"  />
        <result property="noticeContent" column="notice_content"/>
        <result property="status"       column="status"      />
        <result property="bizId"        column="biz_id"      />
        <result property="bizType"      column="biz_type"    />
        <result property="readStatus"   column="read_status" />
        <result property="createBy"     column="create_by"   />
        <result property="createTime"   column="create_time" />
        <result property="updateBy"     column="update_by"   />
        <result property="updateTime"   column="update_time" />
        <result property="remark"       column="remark"      />
    </resultMap>
    
    <sql id="selectNoticeVo">
        select notice_id, notice_title, notice_type, cast(notice_content as char) as notice_content, status, biz_id, biz_type, read_status, create_by, create_time, update_by, update_time, remark 
		from sys_notice
    </sql>
    
    <select id="selectNoticeById" parameterType="Long" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        where notice_id = #{noticeId}
    </select>
    
    <select id="selectNoticeList" parameterType="SysNotice" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        <where>
			<if test="noticeTitle != null and noticeTitle != ''">
				AND notice_title like concat('%', #{noticeTitle}, '%')
			</if>
			<if test="noticeType != null and noticeType != ''">
				AND notice_type = #{noticeType}
			</if>
			<if test="createBy != null and createBy != ''">
				AND create_by like concat('%', #{createBy}, '%')
			</if>
		</where>
    </select>
    
    <insert id="insertNotice" parameterType="SysNotice">
        insert into sys_notice (
			<if test="noticeTitle != null and noticeTitle != '' ">notice_title, </if>
			<if test="noticeType != null and noticeType != '' ">notice_type, </if>
			<if test="noticeContent != null and noticeContent != '' ">notice_content, </if>
			<if test="status != null and status != '' ">status, </if>
			<if test="bizId != null ">biz_id, </if>
			<if test="bizType != null and bizType != '' ">biz_type, </if>
			<if test="readStatus != null and readStatus != '' ">read_status, </if>
			<if test="remark != null and remark != ''">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
			<if test="noticeTitle != null and noticeTitle != ''">#{noticeTitle}, </if>
			<if test="noticeType != null and noticeType != ''">#{noticeType}, </if>
			<if test="noticeContent != null and noticeContent != ''">#{noticeContent}, </if>
			<if test="status != null and status != ''">#{status}, </if>
			<if test="bizId != null ">#{bizId}, </if>
			<if test="bizType != null and bizType != ''">#{bizType}, </if>
			<if test="readStatus != null and readStatus != ''">#{readStatus}, </if>
			<if test="remark != null and remark != ''">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
		)
    </insert>
	 
    <update id="updateNotice" parameterType="SysNotice">
        update sys_notice 
        <set>
            <if test="noticeTitle != null and noticeTitle != ''">notice_title = #{noticeTitle}, </if>
            <if test="noticeType != null and noticeType != ''">notice_type = #{noticeType}, </if>
            <if test="noticeContent != null">notice_content = #{noticeContent}, </if>
            <if test="status != null and status != ''">status = #{status}, </if>
            <if test="bizId != null ">biz_id = #{bizId}, </if>
            <if test="bizType != null and bizType != '' ">biz_type = #{bizType}, </if>
            <if test="readStatus != null and readStatus != '' ">read_status = #{readStatus}, </if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
        </set>
        where notice_id = #{noticeId}
    </update>
	
    <delete id="deleteNoticeById" parameterType="Long">
        delete from sys_notice where notice_id = #{noticeId}
    </delete>
    
    <delete id="deleteNoticeByIds" parameterType="Long">
        delete from sys_notice where notice_id in 
        <foreach item="noticeId" collection="array" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>
    
    <!-- 供应商通知相关查询 -->
    <select id="selectSupplierNotices" parameterType="Long" resultType="java.util.Map">
        select notice_id as noticeId, notice_title as title, notice_content as content, 
               create_time as createTime, read_status as status
        from sys_notice
        where target_user_id = #{supplierId}
        order by create_time desc
    </select>
    
    <select id="selectSupplierNoticeDetail" parameterType="java.util.Map" resultType="java.util.Map">
        select notice_id as noticeId, notice_title as title, notice_content as content, 
               create_time as createTime, read_status as status
        from sys_notice
        where notice_id = #{noticeId} and target_user_id = #{supplierId}
    </select>
    
    <update id="updateNoticeReadStatus" parameterType="java.util.Map">
        update sys_notice
        set read_status = '1'
        where notice_id = #{noticeId} and target_user_id = #{supplierId}
    </update>
    
    <update id="updateAllNoticeReadStatus" parameterType="Long">
        update sys_notice
        set read_status = '1'
        where target_user_id = #{supplierId} and read_status = '0'
    </update>
    
    <select id="countUnreadNoticesBySupplierId" parameterType="Long" resultType="Integer">
        select count(*) from sys_notice
        where target_user_id = #{supplierId} and read_status = '0'
    </select>
    
</mapper>