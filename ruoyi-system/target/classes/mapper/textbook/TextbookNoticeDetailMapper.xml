<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.textbook.mapper.TextbookNoticeDetailMapper">

    <resultMap id="TextbookNoticeDetailResult" type="com.ruoyi.textbook.domain.TextbookNoticeDetail">
        <id property="detailId" column="detail_id"/>
        <result property="noticeId" column="notice_id"/>
        <result property="textbookId" column="textbook_id"/>
        <result property="isbn" column="isbn"/>
        <result property="bookName" column="book_name"/>
        <result property="plannedQty" column="planned_qty"/>
        <result property="collegeId" column="college_id"/>
        <result property="majorId" column="major_id"/>
        <result property="classId" column="class_id"/>
        <result property="className" column="class_name"/>
        <result property="gradeLevel" column="grade_level"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByNoticeId" parameterType="Long" resultMap="TextbookNoticeDetailResult">
        SELECT * FROM textbook_notice_detail
        WHERE notice_id = #{noticeId} AND del_flag = '0'
        ORDER BY detail_id
    </select>

    <insert id="batchInsert" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="detailId">
        INSERT INTO textbook_notice_detail
        (notice_id, textbook_id, isbn, book_name, planned_qty, college_id, major_id, class_id, class_name, grade_level, del_flag, create_by, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.noticeId}, #{item.textbookId}, #{item.isbn}, #{item.bookName}, #{item.plannedQty},
             #{item.collegeId}, #{item.majorId}, #{item.classId}, #{item.className}, #{item.gradeLevel}, '0', #{item.createBy}, NOW())
        </foreach>
    </insert>

    <update id="deleteByNoticeId" parameterType="Long">
        UPDATE textbook_notice_detail SET del_flag = '1' WHERE notice_id = #{noticeId}
    </update>

    <update id="deleteByNoticeIds" parameterType="Long[]">
        UPDATE textbook_notice_detail SET del_flag = '1' WHERE notice_id IN
        <foreach item="noticeId" collection="array" open="(" separator="," close=")">#{noticeId}</foreach>
    </update>

</mapper>
