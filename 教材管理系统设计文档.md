# 教材管理系统 - 系统设计文档

**版本**: v4.0
**更新日期**: 2026-04-16
**技术栈**: Spring Boot 2.x + Vue 2 + ElementUI + MySQL + Redis
**框架**: RuoYi-Vue 3.9.0
**文档状态**: ✅ 生产就绪（含领书通知/领书单完整模块）

---

## 📌 v4.0 更新日志 (2026-04-16)

### ⭐ 核心变更：新增班级领书通知/领书单完整模块

本次更新实现了**教材采购系统_完整流程设计.md**中定义的所有核心业务功能：

| # | 新增模块 | 功能点 | 实现状态 |
|---|---------|--------|:-------:|
| **1** | **领书通知管理** | 发布通知、自动生成领书单、进度跟踪 | ✅ 完成 |
| **2** | **领书单管理** | 分批出库、库存扣减、流水生成 | ✅ 完成 |
| **3** | **打印功能** | 标准A4格式、签名栏、一式三份 | ✅ 完成 |
| **4** | **数据字典** | 学期/学院/专业/紧急程度/状态 | ✅ 完成 |

### 🆕 新增文件清单

**后端（18个文件）：**
- Domain: `BookNotice.java`, `BookClaimForm.java`, `BookClaimFormDetail.java`
- Mapper: 3个接口 + 3个XML映射文件
- Service: 2个接口 + 3个实现类（含事务管理）
- Controller: 2个RESTful控制器（14个API端点）

**前端（6个文件）：**
- Vue页面: `noticeManage/index.vue`, `claimForm/index.vue`
- 打印组件: `claimForm/components/ClaimFormPrint.vue`
- API接口: `noticeManage.js` (7个), `claimForm.js` (7个)

**数据库（3张表）：**
- `book_notice`: 领书通知主表（15字段）
- `book_claim_form`: 领书单表（17字段）
- `book_claim_form_detail`: 领书单明细表（10字段）

### 📊 功能覆盖率统计

| 文档章节 | 要求 | 实现 | 覆盖率 |
|---------|------|------|:-----:|
| §5.1 book_notice表 | 15字段 | 15字段 | **100%** |
| §5.2 book_claim_form表 | 17字段 | 17字段 | **100%** |
| §5.3 book_claim_form_detail表 | 10字段 | 10字段 | **100%** |
| §2.5.1 发布领书通知 | 按班级+校验库存 | 已实现 | **100%** |
| §2.5.2 打印领书单 | 含签名栏标准格式 | 已实现 | **100%** |
| §2.5.3 确认出库 | 支持分批出库 | 已实现 | **100%** |
| §2.5.4 状态流转 | 4种状态自动流转 | 已实现 | **100%** |

**综合覆盖率：100%**

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [技术选型](#3-技术选型)
4. [数据库设计](#4-数据库设计) ⚡️ **v3.0更新**
5. [核心业务流程](#5-核心业务流程) ⚡️ **v3.0增强**
6. [角色权限体系](#6-角色权限体系) ⚡️ **v3.0更新**
7. [接口设计规范](#7-接口设计规范)
8. [安全设计](#8-安全设计) ⚡️ **v3.0重大更新**
9. [Excel导入专项设计](#9-excel导入专项设计) 🆕 **v3.0新增**
10. [通知系统设计](#10-通知系统设计) 🆕 **v3.0新增**
11. [部署架构](#11-部署架构)

---

## 1. 项目概述

### 1.1 项目背景

随着高校教学改革的不断深入，教材管理工作面临着越来越多的挑战。传统的手工管理模式已经无法满足现代高校对教材采购、库存、发放等环节的高效管理需求。

**当前痛点**:
1. **信息孤岛**: 教师、库管员、供应商之间的信息传递依赖纸质单据或即时通讯工具，效率低下且容易出错
2. **库存不透明**: 库存数量实时性差，经常出现"申请了但没货"或"有货但不知道"的情况
3. **流程不规范**: 缺乏统一的购书审核、入库出库标准流程，数据难以追溯
4. **权限混乱**: 不同角色（教师、管理员、供应商）的职责边界不清，存在越权操作风险
5. **统计困难**: 无法快速生成各类报表（如某学期各学院教材使用量、库存预警清单等）

**项目目标**:
基于RuoYi-Vue框架，构建一套完整的教材管理系统，实现：
- 教材信息的数字化管理
- 购书申请→审核→领书的全流程线上化
- 库存实时监控与预警
- 采购入库出库的规范化操作
- 多角色权限隔离与数据安全
- 完整的操作日志与审计追踪

**适用范围**:
本系统适用于高等院校的教材管理部门，支持多学院、多专业、多班级的教材统一管理。

### 1.2 项目目标

#### v1.0 基础目标（已实现）

| 目标编号 | 目标描述 | 实现状态 | 核心功能 |
|---------|---------|:-------:|---------|
| OBJ-1.1 | 教材信息管理 | ✅ 完成 | 教材CRUD、ISBN索引、分类管理 |
| OBJ-1.2 | 购书申请流程 | ✅ 完成 | 在线提交、状态机、通知提醒 |
| OBJ-1.3 | 库存管理 | ✅ 完成 | 实时库存、预警机制、多仓库支持 |
| OBJ-1.4 | 入库出库管理 | ✅ 完成 | 采购入库、领书出库、流水记录 |
| OBJ-1.5 | 基础权限控制 | ✅ 完成 | 角色分配、菜单权限、按钮权限 |

#### v2.0 重构目标（已实现）

| 目标编号 | 目标描述 | 实现状态 | 核心改进 |
|---------|---------|:-------:|---------|
| OBJ-2.1 | 角色体系重构 | ✅ 完成 | 从5角色简化为3角色（教师/库管员/供应商）|
| OBJ-2.2 | 业务流程优化 | ✅ 完成 | 删除冗余角色，明确职责边界 |
| OBJ-2.3 | 供应商模块独立 | ✅ 完成 | 供应商自主管理采购单、确认发货 |
| OBJ-2.4 | 数据隔离初步实现 | ✅ 完成 | 供应商只能看到自己的采购单 |

#### v3.0 安全与质量目标（已实现）

见下方 **1.3 v3.0 新增目标** 章节。

### 1.3 v3.0 新增目标

| 目标 | 说明 |
|------|------|
| **数据一致性保障** | 库存变更只能通过入库/出库，禁止直接编辑，确保流水完整 |
| **防误操作保护** | 已完成的业务单据不可删除/编辑，防止历史数据被破坏 |
| **数据权限隔离** | 教师/供应商严格按身份过滤数据，杜绝越权访问 |
| **导入可靠性** | MD5防重复 + 逐行校验 + 字典验证，保证批量操作质量 |
| **业务聚合优化** | 缺书转采购时按ISBN合并，避免重复采购 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                                │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │  教师端   │  │ 库管员端 │  │ 管理员端 │  │ 供应商端  │  │
│   └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│        │              │              │              │        │
│  ┌─────▼──────────────▼──────────────▼──────────────▼─────┐ │
│  │                   前端展示层 (Vue2 + ElementUI)          │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────┐  │ │
│  │  │教材管理  │ │购书管理  │ │库存管理  │ │通知中心 🆕  │  │ │
│  │  ├─────────┤ ├─────────┤ ├─────────┤ ├─────────────┤  │ │
│  │  │入库管理  │ │出库管理  │ │缺货管理  │ │Excel导入🆕  │  │ │
│  │  ├─────────┤ ├─────────┤ ├─────────┤ ├─────────────┤  │ │
│  │  │采购管理  │ │供应商管理│ │统计分析  │ │ISBN聚合🆕   │  │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────┘  │ │
│  └─────────────────────────┬──────────────────────────────┘ │
│                            │ HTTP/RESTful API               │
│  ┌─────────────────────────▼──────────────────────────────┐ │
│  │                  后端服务层 (Spring Boot 2.x)            │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │              安全拦截层                           │  │ │
│  │  │  JWT认证 → @PreAuthorize → @RepeatSubmit → 参数校验│  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │              业务逻辑层 (Service)                 │  │ │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │  │ │
│  │  │  │购书服务   │ │库存服务   │ │审核服务   │         │  │ │
│  │  │  │+MD5防重🆕 │ │+乐观锁    │ │+状态机   │         │  │ │
│  │  │  └──────────┘ └──────────┘ └──────────┘         │  │ │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │  │ │
│  │  │  │入库服务   │ │出库服务   │ │缺书服务   │         │  │ │
│  │  │  │+流水记录🆕│ │+流水记录🆕│ │+ISBN聚合🆕│         │  │ │
│  │  │  └──────────┘ └──────────┘ └──────────┘         │  │ │
│  │  │  ┌──────────┐ ┌──────────┐                     │  │ │
│  │  │  │通知服务🆕 │ │导入服务🆕 │                     │  │ │
│  │  │  │6种类型   │ │逐行校验   │                     │  │ │
│  │  │  └──────────┘ └──────────┘                     │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │              数据访问层 (MyBatis Mapper)           │  │ │
│  │  │  XML映射 → SQL执行 → 结果映射 → 返回Service       │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └─────────────────────────┬──────────────────────────────┘ │
│                            │ JDBC                           │
│  ┌─────────────────────────▼──────────────────────────────┐ │
│  │                    数据存储层                            │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────┐     │ │
│  │  │ MySQL    │  │  Redis   │  │ 文件存储(可选)    │     │ │
│  │  │ 主数据库  │  │ 缓存     │  │ Excel模板/附件    │     │ │
│  │  └──────────┘  └──────────┘  └──────────────────┘     │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 分层职责

本系统采用经典的**分层架构**模式，共分为5个层次，每个层次职责明确、边界清晰：

#### Layer 1: 用户层（User Interface Layer）

**组成**: 教师端、库管员端、管理员端、供应商端

**职责**:
- 提供用户交互界面（Vue2 + ElementUI）
- 负责数据展示和用户输入收集
- 执行前端校验（格式、必填项等）
- 调用后端API获取/提交数据

**技术栈**: Vue 2.x + Element UI + Axios + Vue Router

---

#### Layer 2: 前端展示层（Presentation Layer）

**组成**: 各业务模块的Vue页面组件

**主要模块**:
```
前端展示层
├── 教材管理模块 (book/)
│   ├── index.vue        # 教材列表
│   └── add/edit.vue     # 新增/编辑表单
├── 购书管理模块 (buy/)
│   ├── index.vue        # 购书列表
│   └── apply.vue        # 申请表单
├── 库存管理模块 (inventory/)
│   └── index.vue        # 库存列表+预警
├── 入库管理模块 (inbound/)
│   └── index.vue        # 入库操作
├── 出库管理模块 (outbound/)
│   └── index.vue        # 出库操作
├── 缺货管理模块 (shortage/)
│   └── index.vue        # 缺书列表
├── 采购管理模块 (purchase/)
│   ├── index.vue        # 采购单列表
│   └── importDialog.vue # Excel导入对话框 🆕 v3.0
├── 供应商管理模块 (supplier/)
│   └── index.vue        # 供应商CRUD
└── 通知中心 🆕 v3.0
    └── notice/index.vue # 通知列表
```

**职责**:
- 实现业务逻辑的前端部分（如表单验证、状态判断）
- 组装API请求参数
- 处理API响应并更新页面状态
- 路由控制和权限守卫

---

#### Layer 3: 后端服务层（Service Layer）

**组成**: Spring Boot Controller + Service + Mapper

**核心Controller**:
```java
// 教材管理
TbBookController          // 教材CRUD

// 购书管理 ⚡️ v3.0增强
TbBuyController           // 购书申请 + 权限隔离(Fix #12)
TbPurchaseController      // 购书单管理 + 删除保护(Fix #9)

// 库存管理 ⚡️ v3.0增强
TbInventoryController     // 库存查询 + 编辑保护(Fix #1)

// 入库出库 🆕 v3.0增强
TbInboundController       // 入库处理(流水+缺书联动)
TbOutboundController      // 出库处理(乐观锁+流水)

// 缺货管理 ⚡️ v3.0增强
TbShortageController      // 缺书处理 + ISBN聚合(Fix #14)

// 供应商管理 ⚡️ v3.0增强
TbSupplierController      // 供应商CRUD + 数据隔离

// 通知管理 🆕 v3.0新增
TbNoticeController        // 通知CRUD + 已读未读
```

**职责**:
- 接收HTTP请求并解析参数
- 调用Service层执行业务逻辑
- 参数校验（JSR-303注解）
- 权限控制（@PreAuthorize）
- 统一异常处理
- 返回标准JSON响应

---

#### Layer 4: 业务逻辑层（Business Logic Layer）

**组成**: Service接口 + Impl实现类

**核心Service**:
```java
// 基础服务
ITbBookService / TbBookServiceImpl           // 教材业务
ITbBuyService / TbBuyServiceImpl             // 购书业务 + Excel导入🆕
ITbPurchaseService / TbPurchaseServiceImpl   // 采购单业务

// 库存相关 🆕 v3.0增强
ITbInventoryService / TbInventoryServiceImpl // 库存业务(安全限制)
ITbInboundService / TbInboundServiceImpl     // 入库业务(流水+联动)
ITbOutboundService / TbOutboundServiceImpl   // 出库业务(乐观锁+流水)
ITbStockLogService / TbStockLogServiceImpl   // 流水日志服务🆕

// 缺货与供应商
ITbShortageService / TbShortageServiceImpl   // 缺书业务(ISBN聚合)🆕
ITbSupplierService / TbSupplierServiceImpl   // 供应商业务

// 通知服务 🆕 v3.0新增
NoticeService                                 // 通知发送(6种类型)
```

**职责**:
- 实现核心业务规则
- 事务管理（@Transactional）
- 跨表数据操作
- 复杂业务计算（如ISBN聚合）
- 调用Mapper层数据访问

---

#### Layer 5: 数据访问层（Data Access Layer）

**组成**: MyBatis Mapper接口 + XML映射文件

**核心Mapper**:
```java
TbBookMapper              // 教材SQL
TbBuyMapper               // 购书SQL + file_hash查询🆕
TbPurchaseMapper          // 采购SQL
TbInventoryMapper         // 库存SQL + 乐观锁更新🆕
TbInboundMapper           // 入库SQL
TbOutboundMapper          // 出库SQL
TbShortageMapper          // 缺书SQL
TbSupplierMapper          // 供应商SQL
TbStockLogMapper          // 流水日志SQL🆕
SysNoticeMapper           // 通知SQL(扩展)🆕
```

**职责**:
- SQL语句编写与优化
- 数据库CRUD操作
- 结果集映射（ResultMap）
- 动态SQL（if/where/foreach）
- 数据库特有语法支持

---

### 分层调用关系图

```
用户操作
    ↓
[Layer 1] 用户层 (浏览器)
    ↓ HTTP/JSON
[Layer 2] 前端展示层 (Vue组件)
    ↓ Axios请求
[Layer 3] 后端服务层 (Controller)
    ↓ 调用
[Layer 4] 业务逻辑层 (Service)
    ↓ 调用
[Layer 5] 数据访问层 (Mapper)
    ↓ JDBC/SQL
[数据库] MySQL + Redis
```

**设计原则**:
- **单向依赖**: 上层依赖下层，下层不依赖上层
- **职责单一**: 每层只做自己的事，不越权
- **可替换性**: 每层实现可独立替换（如换数据库只需改Layer5）

---

## 3. 技术选型

### 3.1 技术栈总览

| 层次 | 技术 | 版本 | 用途说明 |
|------|------|------|---------|
| **前端框架** | Vue.js | 2.x | 渐进式JavaScript框架，组件化开发 |
| **UI组件库** | Element UI | 2.15 | 基于Vue的企业级UI组件库 |
| **HTTP客户端** | Axios | 0.24+ | 前后端通信，请求/响应拦截 |
| **路由管理** | Vue Router | 3.x | 单页面应用路由控制 |
| **状态管理** | Vuex | 3.x | 全局状态管理（可选）|
| **后端框架** | Spring Boot | 2.x | Java企业级应用框架 |
| **安全框架** | Spring Security + JWT | - | 认证鉴权、Token管理 |
| **ORM框架** | MyBatis | 3.5+ | SQL映射框架，灵活SQL控制 |
| **数据库** | MySQL | 5.7+/8.0 | 关系型数据存储 |
| **缓存** | Redis | 6.0+ | 缓存、Session存储、分布式锁 |
| **Excel处理** | Apache POI | 4.1+ | Excel导入导出（v3.0新增）|
| **API文档** | Swagger/Knife4j | 3.0 | 接口文档自动生成 |
| **工具库** | Hutool | 5.x | Java工具类集（MD5等）|

---

### 3.2 技术选型理由

#### 为什么选择 Vue 2 + Element UI？

**优势**:
1. **学习曲线平缓**: 相比React/Angular，Vue更易上手
2. **中文生态完善**: Element UI提供完整的中文文档和社区支持
3. **RuoYi集成**: RuoYi-Vue官方基于此技术栈，开箱即用
4. **组件丰富**: Element UI提供Table/Form/Dialog等企业常用组件
5. **稳定性**: Vue 2已非常成熟，适合企业内部管理系统

**适用场景**:
- 后台管理系统（如本教材管理系统）
- 数据展示密集型应用
- 表单操作频繁的业务系统

---

#### 为什么选择 Spring Boot 2.x？

**优势**:
1. **约定优于配置**: 快速搭建项目，减少XML配置
2. **生态完善**: 与Spring Security/MyBatis无缝集成
3. **微服务就绪**: 可平滑过渡到Spring Cloud微服务架构
4. **企业级特性**: 内置监控、健康检查、外部化配置
5. **RuoYi基础**: RuoYi-Vue基于Spring Boot 2.x构建

**核心依赖**:
```xml
<!-- Spring Boot Starter -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<!-- Spring Security -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>

<!-- MyBatis -->
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
</dependency>

<!-- MySQL Driver -->
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
</dependency>

<!-- Redis -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>

<!-- Apache POI (v3.0新增) -->
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi-ooxml</artifactId>
    <version>4.1.2</version>
</dependency>
```

---

#### 为什么选择 MyBatis 而非 JPA/Hibernate？

**原因**:
1. **SQL可控**: 对于复杂查询和性能优化，手写SQL更灵活
2. **学习成本低**: 相比HQL/Criteria API，SQL更直观
3. **调试方便**: 可直接在数据库执行SQL验证正确性
4. **RuoYi标准**: RuoYi-Vue官方使用MyBatis
5. **动态SQL强大**: `<if>`/`<foreach>`等标签满足复杂业务需求

**适用场景**:
- 需要精细控制SQL性能的系统
- 多表关联查询较多的业务
- 需要使用数据库特有语法（如MySQL的GROUP_CONCAT）

---

#### 为什么选择 MySQL + Redis？

**MySQL（主数据库）**:
- 关系型数据存储（教材、订单、库存等结构化数据）
- 支持事务ACID，保证数据一致性
- 成熟稳定，运维经验丰富
- 开源免费，降低项目成本

**Redis（缓存层）**:
- 缓存热点数据（如字典数据、用户权限）
- 存储JWT Token（支持分布式会话）
- 分布式锁（防止并发重复提交）
- 会话管理（Spring Session）

**数据流转**:
```
请求 → Redis查缓存 → 命中? → 返回
                    ↓ 未命中
                 MySQL查询 → 结果写入Redis → 返回
```

---

### 3.3 v3.0 新增技术选型 🆕

| 技术 | 引入版本 | 应用场景 | 核心功能 |
|------|:-------:|---------|---------|
| **Apache POI** | v3.0 | Excel导入模块 | 解析xlsx/xls文件，读取单元格数据 |
| **MD5算法** | v3.0 | 文件防重复 | 计算文件指纹，32位十六进制哈希值 |
| **乐观锁** | v3.0 | 并发库存控制 | `WHERE stock_num = expected` 无锁更新 |
| **@Transactional** | v3.0 | 事务原子性 | 入库/出库全流程回滚保证 |
| **Spring Events** | v3.0 | 事件驱动通知 | 入库完成后发布事件触发通知 |

**POI Excel处理示例** (v3.0核心代码):
```java
// 使用@Excel注解固定列索引，不依赖表头名称
public class TbPurchaseImportDTO {
    @Excel(name = "ISBN", sort = 0)        // A列
    private String isbn;

    @Excel(name = "教材名称", sort = 1)     // B列
    private String bookName;

    @Excel(name = "采购数量", sort = 2)     // C列
    private Integer quantity;
    // ...
}
```

---

## 4. 数据库设计 ⚡️ v3.0更新

### 4.1 ER关系总览

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  textbook_book│◄───┤textbook_stock│◄───┤tb_stock_log 🆕│
│  (教材信息)   │     │  (库存表)    │     │ (库存流水表)  │
└──────┬───────┘     └──────▲───────┘     └──────────────┘
       │                    │
       ▼                    │
┌──────────────┐     ┌──────┴───────┐     ┌──────────────┐
│textbook_buy  │     │textbook_inbound│   │textbook_outbound│
│ (购书主表)   │────▶│  (入库表)     │   │  (出库表)      │
└──────┬───────┘     └──────────────┘   └──────────────┘
       │
       ├──▶ textbook_purchase_detail (购书明细)
       ├──▶ textbook_lack (缺书表) ◀─── 【ISBN聚合】batchConvertToPurchase
       │                                    │
       ▼                                    ▼
┌──────────────┐                    ┌──────────────┐
│ tb_supplier   │                    │ sys_notice 🆕  │
│ (供应商表)    │                    │ (通知扩展表)   │
└──────────────┘                    └──────────────┘
                                            │
                              ┌─────────────┴─────────────┐
                              │ biz_id / biz_type / read_status │
                              │ target_user_id / user_type    │
                              └───────────────────────────────┘
```

### 4.2 核心表结构 ⚡️ v3.0新增字段标注

#### textbook_buy (购书主表)

|| 字段名 | 类型 | 必填 | 默认值 | 说明 | v3.0变更 |
|--------|------|:----:|--------|------|----------|
| buy_id | BIGINT | PK | 自增 | 主键ID | — |
| book_id | BIGINT | ✅ | — | 关联教材ID | — |
| purchase_no | VARCHAR(30) | ✅ | 自动 | 采购单号(CG+时间戳) | — |
| user_id | BIGINT | ✅ | — | 提交人ID | Fix #12: list自动过滤此字段 |
| user_name | VARCHAR(50) | ✅ | — | 提交人姓名 | — |
| user_type | CHAR(1) | ✅ | '1' | 类型：1教师2库管员 | — |
| dept_name | VARCHAR(100) | ❌ | — | 所属部门 | — |
| buy_num | INT | ✅ | — | 申请数量 | — |
| submit_time | DATETIME | ✅ | NOW() | 提交时间 | — |
| audit_status | CHAR(1) | ✅ | '0' | 审核状态：0待审1通过2驳回 | Fix #9: "1"→禁止删除 |
| audit_user_id | BIGINT | ❌ | — | 审核人ID | — |
| audit_time | DATETIME | ❌ | — | 审核时间 | — |
| reject_reason | VARCHAR(500) | ❌ | — | 驳回原因 | — |
| receive_status | CHAR(1) | ✅ | '0' | 领书状态：0未领1已领 | Fix #9: "1"→禁止删除 |
| receive_time | DATETIME | ❌ | — | 领书时间 | — |
| funding_source | VARCHAR(50) | ❌ | — | 经费来源 | — |
| **file_hash** | **VARCHAR(32)** | **❌** | **NULL** | **导入文件MD5** | **🆕 Fix #8: 防重复** |
| del_flag | CHAR(1) | ✅ | '0' | 删除标记 | — |

#### sys_notice (通知扩展表) 🆕

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 用途 |
|--------|------|:----:|--------|------|------|
| notice_id | BIGINT | PK | 自增 | 通知ID | — |
| notice_title | VARCHAR(50) | ✅ | — | 通知标题 | — |
| notice_type | CHAR(1) | ✅ | — | 通知类型(1-6) | 1审核2缺书3到货4采购5系统6供应商 |
| notice_content | VARCHAR(2000) | ❌ | — | 通知内容 | — |
| **target_user_id** | **BIGINT** | **❌** | **NULL** | **目标用户ID** | **🆕 NULL=全员, 有值=指定用户(Fix #12)** |
| **user_type** | **CHAR(1)** | **❌** | **NULL** | **接收人类型** | **🆕 1教师2库管员3供应商** |
| **biz_id** | **BIGINT** | **❌** | **NULL** | **关联业务ID** | **🆕 如buyId(Fix #13)** |
| **biz_type** | **VARCHAR(20)** | **❌** | **NULL** | **业务类型** | **🆕 如"textbook_buy"(Fix #13)** |
| **read_status** | **CHAR(1)** | **✅** | **'0'** | **阅读状态** | **🆕 0未读1已读** |
| status | CHAR(1) | ✅ | '0' | 发布状态 | — |
| create_by | BIGINT | ❌ | — | 创建者 | — |
| create_time | DATETIME | ✅ | NOW() | 创建时间 | — |

#### tb_stock_log (库存流水日志表) 🆕

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|:----:|------|
| log_id | BIGINT PK | 流水ID |
| book_id | BIGINT ✅ | 教材ID |
| operation_type | VARCHAR(20) ✅ | 操作类型：INBOUND / OUTBOUND |
| change_quantity | INT ✅ | 变动数量（正数增加，负数减少）|
| before_stock | INT ✅ | 变更前库存 |
| after_stock | INT ✅ | 变更后库存 |
| related_order_no | VARCHAR(30) | 关联单号（入库单号/出库单号）|
| operator_id | BIGINT ✅ | 操作人ID |
| operator_name | VARCHAR(50) | 操作人姓名 |
| remark | VARCHAR(500) | 备注 |
| create_time | DATETIME ✅ | 创建时间 |

#### tb_supplier (供应商扩展表)

| 字段名 | 类型 | v3.0新增 | 说明 |
|--------|------|:--------:|------|
| supplier_id | BIGINT PK | — | 供应商ID |
| supplier_name | VARCHAR(100) ✅ | — | 供应商名称 |
| contact_person | VARCHAR(50) | — | 联系人 |
| phone | VARCHAR(20) | — | 电话 |
| address | VARCHAR(255) | — | 地址 |
| **user_id** | **BIGINT** | **🆕** | **关联登录账号ID(Fix #11)** |
| **logistics_company** | **VARCHAR(100)** | **🆕** | **物流公司** |
| **logistics_no** | **VARCHAR(50)** | **🆕** | **物流单号** |
| **ship_time** | **DATETIME** | **🆕** | **发货时间** |
| **ship_status** | **CHAR(1)** | **🆕** | **发货状态** |
| remark | VARCHAR(500) | — | 备注 |

#### book_notice (领书通知表) 🆕 v4.0

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|:----:|------|
| notice_id | BIGINT PK AUTO | ✅ | 通知ID |
| notice_no | VARCHAR(50) UNIQUE | ✅ | 通知编号（LS20260220001格式）|
| semester | VARCHAR(20) | ✅ | 学期（如 2025-2026-2）|
| pickup_start | DATETIME | ✅ | 领取开始时间 |
| pickup_end | DATETIME | ✅ | 领取结束时间 |
| pickup_location | VARCHAR(200) | — | 领取地点 |
| status | CHAR(1) DEFAULT '0' | ✅ | 状态：0草稿/1已发布/2领取中/3已完成 |
| total_classes | INT DEFAULT 0 | — | 班级总数 |
| issued_classes | INT DEFAULT 0 | — | 已出库班级数 |
| create_by | VARCHAR(64) | — | 创建人 |
| create_time | DATETIME | ✅ | 创建时间 |
| update_by | VARCHAR(64) | — | 更新人 |
| update_time | DATETIME | — | 更新时间 |
| del_flag | CHAR(1) DEFAULT '0' | — | 删除标志 |
| remark | VARCHAR(500) | — | 备注 |

**索引设计：**
- PRIMARY KEY (notice_id)
- UNIQUE KEY uk_notice_no (notice_no)
- KEY idx_status (status)
- KEY idx_semester (semester)

---

#### book_claim_form (领书单表) 🆕 v4.0

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|:----:|------|
| form_id | BIGINT PK AUTO | ✅ | 领书单ID |
| form_no | VARCHAR(50) UNIQUE | ✅ | 领书单号（自动生成）|
| notice_id | BIGINT FK | ✅ | 关联领书通知ID |
| college_id | BIGINT | — | 学院ID |
| major_id | BIGINT | — | 专业ID |
| class_id | BIGINT | — | 班级ID |
| class_name | VARCHAR(100) | ✅ | 班级名称 |
| status | CHAR(1) DEFAULT '0' | ✅ | 状态：0待领取/1部分出库/2已出库 |
| planned_qty | INT DEFAULT 0 | — | 应发总数（所有教材合计）|
| issued_qty | INT DEFAULT 0 | — | 实发总数 |
| receiver_name | VARCHAR(50) | — | 领书人姓名（班委签名）|
| issue_time | DATETIME | — | 出库时间 |
| create_by | VARCHAR(64) | — | 创建人 |
| create_time | DATETIME | ✅ | 创建时间 |
| update_by | VARCHAR(64) | — | 更新人 |
| update_time | DATETIME | — | 更新时间 |
| del_flag | CHAR(1) DEFAULT '0' | — | 删除标志 |
| remark | VARCHAR(500) | — | 备注 |

**索引设计：**
- PRIMARY KEY (form_id)
- UNIQUE KEY uk_form_no (form_no)
- KEY idx_notice_id (notice_id)
- KEY idx_class_id (class_id)
- KEY idx_status (status)

---

#### book_claim_form_detail (领书单明细表) 🆕 v4.0

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|:----:|------|
| detail_id | BIGINT PK AUTO | ✅ | 明细ID |
| form_id | BIGINT FK | ✅ | 关联领书单ID |
| textbook_id | BIGINT | ✅ | 教材ID |
| isbn | VARCHAR(30) | — | ISBN |
| book_name | VARCHAR(200) | ✅ | 教材名称 |
| author | VARCHAR(100) | — | 作者 |
| publisher | VARCHAR(100) | — | 出版社 |
| price | DECIMAL(10,2) | — | 定价 |
| planned_qty | INT DEFAULT 0 | ✅ | 应发数量 |
| issued_qty | INT DEFAULT 0 | — | 实发数量 |
| create_time | DATETIME | ✅ | 创建时间 |

**索引设计：**
- PRIMARY KEY (detail_id)
- KEY idx_form_id (form_id)
- KEY idx_textbook_id (textbook_id)

---

## 5. 核心业务流程 ⚡️ v3.0增强

### 5.1 完整领书流程（教师 → 库管员）

```
教师提交领书申请
    ↓
系统记录（状态：待审核）
    ↓
库管员收到待审核列表
    ↓
  ┌─ 库存充足 → 审核通过 → 通知教师
  │       ↓
  │   库管员确认出库 ← processOutbound()
  │       │
  │       ├── 创建出库记录(tb_outbound)
  │       ├── 更新库存(乐观锁扣减)
  │       │   UPDATE textbook_stock SET stock_num = stock_num - #{num}
  │       │   WHERE book_id = ? AND stock_num = #{expected}
  │       │   └── 影响行数=0 → 抛出并发冲突异常
  │       ├── 生成流水日志(tb_stock_log) 🆕
  │       │   ├── operation_type = "OUTBOUND"
  │       │   ├── before_stock / after_stock
  │       │   └── 关联 buy_id
  │       └── 发送通知给申请人 🆕
  │           "您申请的《XXX》已出库，请及时领取"
  │       ↓
  │   通知教师领书
  │       ↓
  │   教师到书库领书（线下）
  │
  └─ 库存不足 → 审核驳回 + 通知教师（附原因）
          ↓
      教师可登记缺书 → 系统通知库管员 → 进入采购流程
```

### 5.2 完整缺书采购流程

```
教师 / 库管员登记缺书
    ↓
系统自动发送缺书通知给库管员
    ↓
库管员查看缺书单
    ↓
一键转采购单（或 Excel 批量导入）
    ↓
  ┌─ 手动创建 → 单条录入
  │
  └─ 批量转换 ← batchConvertToPurchase() 🆕 Fix #14
      │
      ├── 输入: N条缺书记录ID
      ├── 过滤: 不存在/已处理 → skippedCount
      ├── 按 ISBN 分组 (Collectors.groupingBy)
      │   ├── ISBN-A: 3条 → quantity求和
      │   └── ISBN-B: 1条 → 原始数量
      ├── 生成 1个采购主单 + M个聚合明细(M≤N)
      └── 批量更新 N条缺书记录 handle_status="1"

采购单生成（状态：待采购）
    ↓
库管员推进状态 → 采购中
    ↓
供应商收到采购单 → 备货发货 → 确认发货 ← confirmShip() 🆕
    │
    ├── 填写物流公司、物流单号
    ├── 更新 ship_status
    └── 通知库管员"已发货"
    ↓
库管员确认到货（状态：已到货）
    ↓
库管员确认入库 ← processInbound() 🆕 增强
    │
    ├── 生成入库单号(INB+时间戳)
    ├── 循环处理明细:
    │   ├── 创建入库记录(tb_inbound)
    │   ├── 更新库存(乐观锁增加)
    │   │   UPDATE SET stock_num = stock_num + #{num}
    │   │   WHERE book_id = ? AND stock_num = #{expected}
    │   ├── 生成流水日志(tb_stock_log) 🆕
    │   │   └── operation_type = "INBOUND"
    │   ├── 【缺书联动】检查等待此教材的缺书单 🆕
    │   │   ├── 入库量 >= 缺书量 → handleStatus="2"(已解决)
    │   │   └── 入库量 < 缺书量 → 部分满足，更新lackNum
    │   ├── 【订单联动】重新开放被驳回的待采购订单 🆕
    │   │   └── auditStatus 从 "2" 回退到 "0"
    │   └── 【双通道通知】🆕
    │       ├── 全局通知（所有相关教师）
    │       └── 供应商通知（确认入库完成）
    └── @Transactional 保证全部原子性

自动增加库存 + 生成流水 + 通知供应商 + 更新缺书单状态
    ↓
教师收到通知：缺书已到货，可重新提交领书申请
```

### 5.3 Excel 导入采购单专项流程 🆕

```
库管员点击"下载导入模板"
    ↓
获取标准模板（固定列、带下拉框和批注）
    ↓
各学院填写采购数据（离线操作）
    ↓
库管员上传 Excel 文件
    ↓
前端校验（文件格式 .xlsx/.xls、大小 ≤10MB）
    ↓
后端 importFromExcel() 接收 MultipartFile
    ↓
【Step 1: MD5防重复检查】🆕 Fix #8
    │
    ├── calculateFileMD5(file) → 计算32位十六进制MD5
    ├── selectByFileHash(md5) → 查询是否已导入
    │   └── 命中 → throw ServiceException("该文件已导入过...")
    └── 未命中 → 继续下一步
    ↓
【Step 2: 解析Excel】
    │
    ├── ExcelUtil.importData(file.getInputStream(), TbPurchaseImportDTO.class)
    │   └── 按 @Excel(sort=0..5) 固定列下标读取（不匹配表头！）
    └── 得到 List<TbPurchaseImportDTO> dataList
    ↓
【Step 3: 逐行校验】
    │
    for each row in dataList:
    │   ├── 设置 rowIndex (从2开始，跳过表头)
    │   ├── 校验 ISBN: 10位或13位纯数字
    │   │   └── 失败 → errorMsg += "ISBN格式不正确；"
    │   ├── 校验数量: 1-9999整数
    │   │   └── 失败 → errorMsg += "采购数量必须在1-9999之间；"
    │   ├── 校验ISBN是否存在:
    │   │   ├── selectByIsbn(isbn) → null?
    │   │   └── 失败 → errorMsg += "系统中不存在该ISBN对应的教材[isbn]；"
    │   ├── 校验学院字典: validateDictValue("tb_college", college)
    │   │   └── 失败 → errorMsg += "申请学院[xxx]不在系统字典中；"
    │   ├── 校验专业字典: validateDictValue("tb_major", major)
    │   │   └── 失败 → errorMsg += "申请专业[xxx]不在系统字典中；"
    │   ├── 全部通过 → successList.add(row)
    │   └── 有失败 → failList.add(row) + continue（不阻断！）
    ↓
【Step 4: 结果判断】
    │
    ├── successCount == 0 → return error("全部失败")
    └── successCount > 0 → 继续
    ↓
【Step 5: @Transactional 事务执行】
    │
    ├── 生成采购主单
    │   ├── purchaseNo = CG + yyyyMMddHHmmss + seq
    │   ├── fundingSource = school
    │   ├── fileHash = md5值 🆕 Fix #8
    │   └── insertTbPurchase(purchase)
    │
    ├── 逐行生成采购明细
    │   for each row in successList:
    │   ├── detail.setBookId(从ISBN查出的bookId)
    │   ├── detail.setQuantity(row.quantity)
    │   └── insertTbPurchaseDetail(detail)
    │
    └── 记录操作日志
        └── LogUtils.info("采购单导入完成...", ...)
    ↓
返回结果汇总:
{
  totalRows: N,
  successCount: S,
  failCount: F,
  purchaseNo: "CG...",
  errorList: [...]
}
```

---

## 6. 角色权限体系 ⚡️ v3.0更新

### 6.1 三角色模型

本系统采用**三角色模型**（v2.0重构后的简化版本），相比v1.0的五角色模型更加清晰高效：

#### 角色定义

| 角色 | 英文标识 | 账号示例 | 定位 | 用户群体 |
|------|---------|---------|------|---------|
| **超级管理员** | admin | `admin` | 系统管理者 | 信息中心技术人员 |
| **教师** | teacher | `teacher` | 教材使用者 | 全体授课教师 |
| **库管员** | warehouseman | `textbook_admin` | 业务操作者 | 教材科/图书馆工作人员 |
| **供应商** | supplier | `supplier` | 外部合作方 | 图书供应商/出版社 |

---

#### v1.0 原始五角色模型（已废弃）

在v1.0版本中，系统设计了5个角色：

| 角色 | v1.0职责 | 废弃原因 |
|------|---------|---------|
| 管理员 | 系统管理+全部业务 | ✅ 保留为admin |
| 教师 | 提交购书申请 | ✅ 保留为teacher |
| **学生** | 查看教材信息、提交需求 | ❌ 删除：与教师功能重叠，增加复杂度 |
| **发行员** | 教材发放管理 | ❌ 删除：合并入库管员，减少审批层级 |
| **采购员** | 采购单管理 | ❌ 删除：合并入库管员，统一操作入口 |

**重构理由**:
1. **简化流程**: 减少角色数量可以缩短审批链路
2. **降低成本**: 少一个角色就少一套权限配置
3. **职责清晰**: 避免功能重叠导致的推诿
4. **易维护**: 新人上手更快，培训成本低

---

#### v2.0 三角色详细说明

##### ① 超级管理员 (admin)

**核心定位**: 系统的"上帝视角"，拥有所有权限

**主要职责**:
- 用户管理：创建/编辑/删除账号、重置密码
- 角色管理：分配角色、配置菜单权限
- 数据查看：可查看所有业务数据（全局视角）
- 系统配置：字典管理、参数设置
- 监控运维：在线用户、操作日志、数据监控

**典型场景**:
```
开学前 → 批量创建教师账号 → 分配teacher角色
学期中 → 监控系统运行状态 → 处理异常
学期末 → 导出统计报表 → 归档数据
```

**权限范围**: 
- ✅ 系统管理模块全部功能
- ✅ 所有业务模块的CRUD + 审核 + 删除
- ✅ 统计分析（全局数据）
- ⚠️ 一般不直接参与日常业务操作

---

##### ② 教师 (teacher)

**核心定位**: 教材的"消费者"，系统的最终用户

**主要职责**:
- 浏览教材目录：查看可用教材及库存情况
- 提交购书申请：为新学期课程申请所需教材
- 查看个人订单：跟踪申请进度（待审→已审→已领）
- 确认领书：审核通过后到书库领取教材
- 接收通知：查看系统推送的各类通知消息

**典型场景**:
```
开学前2周 → 登录系统 → 浏览教材列表
         → 为所教课程选择教材 → 填写数量 → 提交申请
         → 等待库管员审核...
审核通过后 → 收到通知 → 到书库领书 → 确认领取
```

**权限范围** (🆕 v3.0增强):
- ✅ 教材管理：只读（可查看列表和详情）
- ✅ 购书管理：仅"我的购书"（**数据隔离** Fix #12）
- ✅ 库存管理：只读（可查看当前库存）
- ❌ 入库/出库管理：无权限
- ❌ 采购/供应商管理：无权限
- ✅ 通知中心：查看自己的通知
- ✅ 统计分析：仅个人数据

**安全限制**:
- `GET /textbook/buy/list` 自动注入userId，只能看到自己的订单
- 无法通过修改请求参数越权查看他人订单

---

##### ③ 库管员 (warehouseman) ⭐ 核心业务角色

**核心定位**: 教材管理的"执行者"，承担大部分业务操作

**主要职责**:

**a. 教材基础管理**
- 教材CRUD：新增/编辑/删除教材信息
- 库存管理：查看库存、设置预警阈值
  - ⚠️ **v3.0限制**: 不能直接编辑库存数量（Fix #1）
  - ✅ 只能通过入库/出库变更库存

**b. 购书审核**
- 审核教师的购书申请（通过/驳回）
- 驳回时填写原因，触发缺书登记
- ⚠️ **v3.0限制**: 已审核/已领书的订单不可删除（Fix #9）

**c. 入库管理**
- 采购到货登记：确认供应商发货并入库
- 🆕 **v3.0增强**: 自动生成流水日志、缺书联动、双通道通知

**d. 出库管理**
- 发放教材给教师：确认领书出库
- 🆕 **v3.0增强**: 乐观锁防并发、流水记录、通知教师

**e. 缺货处理**
- 查看缺书记录
- 手动创建采购单 或 Excel批量导入 🆕 v3.0
- 缺书转采购（ISBN聚合）🆕 v3.0 Fix #14

**f. 供应商管理**
- 供应商CRUD
- 查看所有采购单（全局视角）

**g. 通知发布**
- 发布全局通知（如"本周五下午停止发书"）

**典型工作流**:
```
每日例行:
├── 上午: 查看"待审核"列表 → 审批教师申请
├── 下午: 处理入库（采购到货）→ 处理出库（教师领书）
├── 随时: 查看缺书清单 → 转采购或导入Excel
└── 周末: 统计本周出入库量 → 生成报表
```

**权限范围**:
- ✅ 教材管理：完整CRUD
- ✅ 购书管理：查看全部 + 审核 + 删除（受限）
- ✅ 库存管理：查看 + 编辑（受限，不能改库存数）
- ✅ 入库/出库管理：完整操作权限
- ✅ 缺货管理：完整权限（含聚合转采购）
- ✅ 采购/供应商管理：完整CRUD
- ✅ 通知管理：发布通知
- ✅ 统计分析：全局业务数据
- ❌ 系统管理：无权限（不能管理用户/角色）

---

##### ④ 供应商 (supplier)

**核心定位**: 外部协作角色，有限的系统访问权限

**主要职责**:
- 查看待入库通知：收到"请发货"的通知
- 查看自己的采购单：🆕 **v3.0数据隔离**（只能看到发给自己的）
- 确认发货：填写物流信息（公司、单号、时间）
- 查看入库完成通知：确认对方已收货

**典型场景**:
```
供应商登录 → 查看"我的采购单"
          → 找到待发货的订单（如《Java程序设计》×50本）
          → 点击"确认发货"
          → 填写：顺丰速运 / SF1234567890 / 2026-04-15 10:30
          → 系统自动通知库管员"已发货"
          → ...等待库管员确认入库...
          → 收到"入库完成"通知 → 该笔订单结束
```

**权限范围** (🆕 v3.0严格限制):
- ❌ 教材管理：无权限
- ❌ 购书管理：无权限
- ❌ 库存管理：无权限
- ❌ 入库/出库管理：无权限
- ✅ 供应商管理：⚠️ 仅查看自己的采购单（myPurchaseList）
- ✅ 确认发货：填写物流信息
- ✅ 通知中心：查看供应商类型的通知
- ✅ 统计分析：仅供应商相关数据

**安全机制**:
```sql
-- 供应商查询时自动过滤
SELECT * FROM textbook_buy 
WHERE supplier_id = 当前登录供应商ID
-- 供应商A永远看不到供应商B的采购单
```

---

#### 角色对比总结表

| 能力 | admin | teacher | warehouseman | supplier |
|:----:|:-----:|:-------:|:------------:|:--------:|
| 系统管理 | ✅ | ❌ | ❌ | ❌ |
| 教材CRUD | ✅ | 只读 | ✅ | ❌ |
| 提交购书申请 | ✅ | ✅ | ✅ | ❌ |
| 审核订单 | ✅ | ❌ | ✅ | ❌ |
| 入库操作 | ✅ | ❌ | ✅ | ❌ |
| 出库操作 | ✅ | ❌ | ✅ | ❌ |
| Excel导入 | ✅ | ❌ | ✅ | ❌ |
| 缺书转采购 | ✅ | ❌ | ✅ | ❌ |
| 供应商CRUD | ✅ | ❌ | ✅ | ❌ |
| **查看自己的采购单** | ✅(全部) | ❌ | ✅(全部) | **✅(仅自己)** |
| **确认发货** | ✅ | ❌ | ❌ | **✅** |
| 数据范围 | 全局 | 个人 | 全局业务 | 个人 |

### 6.2 权限矩阵

详见[《教材管理系统-测试账号手册.md》](./教材管理系统-测试账号手册.md) 第3节。

### 6.3 数据权限隔离策略

| 角色 | 隔离方式 | 实现位置 | 过滤字段 |
|------|---------|---------|---------|
| **教师** | 后端自动注入 | `TbBuyController.list()` | `userId` |
| **供应商** | 后端SQL过滤 | `TbSupplierController.myPurchaseList()` | `supplierId` |
| **库管员/管理员** | 无限制 | — | — |

---

## 7. 接口设计规范

详见[《教材管理系统 API 文档.md》](./教材管理系统 API 文档.md)。

---

## 8. 安全设计 ⚡️ v3.0重大更新

### 8.1 安全规则清单（14项）

| # | 规则类别 | 具体措施 | 实现位置 | 风险等级 |
|---|---------|---------|---------|:--------:|
| 1 | **库存安全** | edit()对比新旧stockNum，不同则拒绝 | TbInventoryController | 🔴 高 |
| 2 | **事务安全** | 出库/入库@Transactional(rollbackFor) | ServiceImpl | 🔴 高 |
| 3 | **解析安全** | @Excel(sort=0..5)固定列索引，不读表头 | DTO | 🟡 中 |
| 4 | **容错安全** | 逐行try-catch收集错误，不中断整批 | importFromExcel() | 🟡 中 |
| 5 | **逻辑安全** | 导入只生成采购单，绝不改库存 | importFromExcel() | 🔴 高 |
| 6 | **容错安全** | ISBN不存在跳过该行，标记原因继续 | importFromExcel() | 🟡 中 |
| 7 | **数据质量** | 学院/专业必须字典枚举校验 | validateDictValue() | 🟡 中 |
| **8** | **防重复** | **MD5文件指纹+file_hash持久化** | **importFromExcel()** | **🔴 高** |
| **9** | **数据完整性** | **已审核/已领书订单禁止删除** | **Controller x2** | **🔴 高** |
| 10 | **并发安全** | 乐观锁WHERE stock_num=expectedStock | Inbound/OutboundServiceImpl | 🔴 高 |
| 11 | **数据隔离** | 供应商只看自己采购单 | myPurchaseList() | 🔴 高 |
| **12** | **数据隔离** | **教师list自动注入userId** | **TbBuyController.list()** | **🟡 中** |
| 13 | **可追溯性** | 通知带biz_id/biz_type关联业务 | NoticeService | 🟡 中 |
| **14** | **业务优化** | **缺书转采购按ISBN聚合** | **batchConvertToPurchase()** | **🟡 中** |

### 8.2 安全防护层次

```
请求进入
  ↓
┌─ Layer 1: 认证鉴权
│   JWT Token → SecurityUtils → LoginUser
│   @PreAuthorize("@ss.hasPermi('...')")
│
├─ Layer 2: 输入校验
│   @Valid / @NotNull / @Size 等JSR-303注解
│   文件格式白名单(.xlsx/.xls)
│   文件大小限制(≤10MB)
│
├─ Layer 3: 业务规则校验
│   Fix #1: 库存数值对比拦截
│   Fix #9: 订单状态双重校验
│   Fix #8: MD5文件指纹查重
│   Fix #12: userId自动注入过滤
│
├─ Layer 4: 数据操作保护
│   @Transactional(rollbackFor) 事务原子性
│   乐观锁(WHERE stock_num=expected) 并发安全
│   逐行try-catch 容错处理
│
└─ Layer 5: 审计追踪
    tb_stock_log 流水日志（每次库存变动必记）
    sys_notice 通知记录（每步业务推进必记）
    操作日志(@Log注解自动记录）
```

---

## 9. Excel导入专项设计 🆕 v3.0新增

### 9.1 设计原则

| 原则 | 实现 |
|------|------|
| **固定列索引** | 使用 `@Excel(sort=N)` 注解，硬编码列位置，不依赖表头文本 |
| **逐行容错** | try-catch包裹每行校验，错误收集到errorList，不影响其他行 |
| **字典强校验** | 学院/专业必须匹配sys_dict_data中的合法值 |
| **防重复** | 文件MD5指纹存入file_hash，二次上传直接拒绝 |
| **事务原子** | @Transactional包裹整个生成过程，失败则全部回滚 |
| **仅生单据** | 导入只创建采购单和明细，绝不触碰库存数据 |

### 9.2 固定列定义

```java
public class TbPurchaseImportDTO {
    @Excel(name = "ISBN", sort = 0)        // A列 - 必填
    private String isbn;

    @Excel(name = "教材名称", sort = 1)     // B列 - 选填
    private String bookName;

    @Excel(name = "采购数量", sort = 2)     // C列 - 必填
    private Integer quantity;

    @Excel(name = "申请学院", sort = 3)     // D列 - 必填
    private String college;

    @Excel(name = "申请专业", sort = 4)     // E列 - 必填
    private String major;

    @Excel(name = "备注", sort = 5)         // F列 - 选填
    private String remark;
}
```

### 9.3 MD5防重复实现

```java
// Step 1: 计算文件MD5
private static String calculateFileMD5(MultipartFile file) throws Exception {
    MessageDigest md = MessageDigest.getInstance("MD5");
    try (InputStream is = file.getInputStream();
         DigestInputStream dis = new DigestInputStream(is, md)) {
        byte[] buffer = new byte[8192];
        while (dis.read(buffer) != -1) { }
    }
    byte[] digest = md.digest();
    StringBuilder sb = new StringBuilder();
    for (byte b : digest) {
        sb.append(String.format("%02x", b & 0xff));
    }
    return sb.toString();  // 例: a1b2c3d4e5f6...
}

// Step 2: 查重
String fileMd5 = calculateFileMD5(file);
TbPurchase existing = tbPurchaseMapper.selectByFileHash(fileMd5);
if (existing != null) {
    throw new ServiceException("该文件已导入过（MD5: " +
        fileMd5.substring(0, 8) + "...），采购单号：" +
        existing.getPurchaseNo());
}

// Step 3: 存储MD5
purchase.setFileHash(fileMd5);  // 写入 textbook_buy.file_hash
```

### 9.4 字典校验实现

```java
private SysDictData validateDictValue(String dictType, String dictLabel) {
    if (StringUtils.isEmpty(dictType) || StringUtils.isEmpty(dictLabel))
        return null;
    SysDictData query = new SysDictData();
    query.setDictType(dictType);      // "tb_college" 或 "tb_major"
    query.setDictLabel(dictLabel);    // 用户输入的学院/专业名称
    query.setStatus("0");             // 只查启用的字典项
    List<SysDictData> dictList = dictDataMapper.selectDictDataList(query);
    return (dictList != null && !dictList.isEmpty()) ? dictList.get(0) : null;
}
```

---

## 10. 通知系统设计 🆕 v3.0新增

### 10.1 通知类型体系

| 类型码 | 类型名称 | 触发场景 | 接收人 | 关联业务 |
|:------:|---------|---------|:------:|---------|
| **1** | **审核通知** | 审核/驳回操作 | 申请人(教师) | textbook_buy |
| **2** | **缺书通知** | 库存不足触发缺书登记 | 相关教师 | textbook_lack |
| **3** | **到货通知** | 入库操作完成后 | 等缺书的教师 | textbook_inbound |
| **4** | **采购通知** | 采购单生成/状态变更 | 库管员 | textbook_buy |
| **5** | **系统通知** | 系统公告/警告 | 全员或指定角色 | — |
| **6** | **供应商通知** | 发货确认/入库完成 | 供应商 | tb_supplier |

### 10.2 通知数据流

```
业务事件发生（如：审核通过）
    ↓
NoticeService.sendNotice(params)
    ↓
构建 SysNotice 对象:
    ├── noticeTitle = "您的购书申请已通过审核"
    ├── noticeType = "1" (审核通知)
    ├── noticeContent = "...具体内容..."
    ├── targetUserId = 申请人userId        ← Fix #12: 定向推送
    ├── userType = "1" (教师)              ← Fix #12: 角色标识
    ├── bizId = buyId                      ← Fix #13: 业务关联
    ├── bizType = "textbook_buy"           ← Fix #13: 业务类型
    └── readStatus = "0" (未读)
    ↓
插入 sys_notice 表
    ↓
前端轮询/WebSocket → 展示通知列表
    ├── 未读角标显示数字 (GET /unreadCount)
    └── 点击标记已读 (PUT /markRead/{id})
```

### 10.3 数据权限控制

```sql
-- 教师查询通知：只看发给自己的
SELECT * FROM sys_notice 
WHERE (target_user_id = #{currentUserId} OR target_user_id IS NULL)
AND del_flag = '0'
ORDER BY create_time DESC

-- 供应商查询通知：只看供应商类型的
WHERE user_type = '3' OR target_user_id = #{currentSupplierId}
```

---

## 11. 部署架构

### 11.1 推荐部署方案

#### 方案A: 单机部署（开发/测试环境）

适用于：开发调试、功能测试、小规模试用（<100用户）

**架构图**:
```
[用户浏览器]
    ↓ HTTP/HTTPS
[Nginx (可选) → 反向代理 + 静态资源]
    ↓
[Spring Boot 应用] ← 端口 8080
    ├── [MySQL 5.7+/8.0] ← 端口 3306
    └── [Redis 6.0+] ← 端口 6379
```

**服务器配置要求**:
| 资源 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2核 | 4核 |
| 内存 | 4GB | 8GB |
| 硬盘 | 50GB SSD | 100GB SSD |
| 操作系统 | CentOS 7.9+ / Ubuntu 20.04 LTS | 同左 |
| JDK | 1.8+ (推荐1.8.0_321+) | 同左 |

**部署步骤**:
```bash
# 1. 安装基础环境
yum install -y java-1.8.0-openjdk-devel mysql-server redis

# 2. 初始化数据库
mysql -u root -p < sql/ry_202xxxxx.sql          # RuoYi基础库
mysql -u root -p < sql/upgrade_textbook_v4.sql   # 教材管理库

# 3. 配置Redis
vim /etc/redis.conf
# 设置密码: requirepass your_password
# 启动: systemctl start redis

# 4. 部署后端
cd /opt/textbook-system
nohup java -jar ruoyi-admin.jar --spring.profiles.active=prod > app.log 2>&1 &

# 5. 部署前端（Nginx）
cp -r dist/* /usr/share/nginx/html/textbook/
nginx -s reload
```

---

#### 方案B: 分布式部署（生产环境）

适用于：正式上线、多用户并发（100-1000+用户）

**架构图**:
```
                    ┌─────────────┐
                    │  负载均衡器  │
                    │   (Nginx)   │
                    │  / LB层     │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              ↓            ↓            ↓
      [Spring Boot #1] [Spring Boot #2] [Spring Boot #3]
      (8080端口)       (8080端口)       (8080端口)
              │            │            │
              └────────────┼────────────┘
                           ↓
              ┌────────────┴────────────┐
              ↓                         ↓
      [MySQL 主从集群]              [Redis Cluster]
      ├─ Master (写)                ├─ Master
      └─ Slave ×2 (读)              └─ Slave ×2
```

**服务器配置**:
| 角色 | 数量 | 配置 | 用途 |
|------|:----:|------|------|
| Nginx LB | 1台 | 2核4GB | 负载均衡、SSL终止、静态资源 |
| Spring Boot | 2-3台 | 4核8GB | 业务应用（可水平扩展）|
| MySQL Master | 1台 | 8核16GB | 主数据库（写入）|
| MySQL Slave | 2台 | 4核8GB | 从库（读取、备份）|
| Redis | 3台 | 2核4GB | 缓存集群（哨兵模式）|

**高可用保障**:
- **负载均衡**: Nginx upstream轮询，健康检查
- **数据库**: 主从复制 + 自动故障转移
- **缓存**: Redis Sentinel 哨兵模式
- **应用**: 多实例部署，单点故障不影响整体
- **监控**: Prometheus + Grafana 监控各项指标

---

### 11.2 Docker容器化部署（推荐）

#### Docker Compose 编排文件

```yaml
version: '3.8'

services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: textbook-mysql
    environment:
      MYSQL_ROOT_PASSWORD: your_strong_password
      MYSQL_DATABASE: ry-vue
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/data:/var/lib/mysql
      - ./sql:/docker-entrypoint-initdb.d
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    restart: unless-stopped

  # Redis 缓存
  redis:
    image: redis:6.2-alpine
    container_name: textbook-redis
    command: redis-server --requirepass your_redis_password
    ports:
      - "6379:6379"
    volumes:
      - ./redis/data:/data
    restart: unless-stopped

  # Spring Boot 后端
  app:
    image: openjdk:8-jre
    container_name: textbook-app
    working_dir: /app
    volumes:
      - ./jar:/app
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      MYSQL_HOST: mysql
      REDIS_HOST: redis
    command: java -jar -Xms512m -Xmx1024m ruoyi-admin.jar
    depends_on:
      - mysql
      - redis
    restart: unless-stopped

  # Nginx 前端
  nginx:
    image: nginx:alpine
    container_name: textbook-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./dist:/usr/share/nginx/html/textbook
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - app
    restart: unless-stopped
```

**快速启动命令**:
```bash
# 1. 构建前端项目
cd ruoyi-ui
npm run build:prod

# 2. 初始化数据库
cp sql/*.sql docker/sql/

# 3. 复制JAR包
cp ruoyi-admin/target/ruoyi-admin.jar docker/jar/

# 4. 启动所有服务
cd docker
docker-compose up -d

# 5. 查看日志
docker-compose logs -f app
```

---

### 11.3 环境变量配置

#### application-prod.yml 关键配置

```yaml
server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: ${MYSQL_PASSWORD}  # 从环境变量读取
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  redis:
    host: ${REDIS_HOST:localhost}
    port: 6379
    password: ${REDIS_PASSWORD:}
    database: 0
    timeout: 10s
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

# JWT配置
jwt:
  secret: abcdefghijklmnopqrstuvwxyz  # 生产环境请修改！
  expiration: 28800000  # 8小时（毫秒）

# 文件上传路径
ruoyi:
  profile: /data/textbook/upload
```

---

### 11.4 性能优化建议

#### 数据库优化

```sql
-- 1. 添加合适的索引
CREATE INDEX idx_buy_user_id ON textbook_buy(user_id);
CREATE INDEX idx_buy_audit_status ON textbook_buy(audit_status);
CREATE INDEX idx_stock_book_id ON textbook_stock(book_id);
CREATE INDEX idx_inbound_order_no ON textbook_inbound(inbound_no);
CREATE INDEX idx_notice_target_user ON sys_notice(target_user_id);

-- 2. 分区表（大数据量时考虑）
-- 按月分区tb_stock_log表
ALTER TABLE tb_stock_log PARTITION BY RANGE (YEAR(create_time)*100 + MONTH(create_time)) (
    PARTITION p202601 VALUES LESS THAN (202602),
    PARTITION p202602 VALUES LESS THAN (202603),
    ...
);

-- 3. 读写分离配置（主从架构）
-- 在MyBatis配置中指定数据源路由
```

#### 缓存策略

```java
// 热点数据缓存示例
@Cacheable(value = "textbook:book", key = "#bookId")
public TbBook getBookById(Long bookId) {
    return tbBookMapper.selectTbBookByBookId(bookId);
}

// 字典数据缓存（启动时加载）
@PostConstruct
public void initDictCache() {
    List<SysDictData> dictList = dictDataMapper.selectDictDataList(new SysDictData());
    dictList.forEach(d -> redisTemplate.opsForValue().set(
        "dict:" + d.getDictType() + ":" + d.getDictValue(), 
        d, 
        24, TimeUnit.HOURS  // 缓存24小时
    ));
}
```

#### JVM调优参数

```bash
java -Xms512m \           # 初始堆内存
     -Xmx1024m \          # 最大堆内存
     -XX:+UseG1GC \       # 使用G1垃圾收集器（低延迟）
     -XX:MaxGCPauseMillis=200 \  # GC暂停目标<200ms
     -XX:+HeapDumpOnOutOfMemoryError \
     -XX:HeapDumpPath=/data/textbook/logs/heapdump.hprof \
     -Djava.security.egd=file:/dev/./urandom \
     -jar ruoyi-admin.jar
```

---

### 11.5 安全加固清单

| 项目 | 配置项 | 建议值 | 说明 |
|------|--------|-------|------|
| **HTTPS** | SSL证书 | Let's Encrypt免费证书 | 强制HTTPS，HTTP自动跳转 |
| **JWT密钥** | jwt.secret | 至少32位随机字符串 | 定期更换，不可泄露 |
| **数据库密码** | spring.datasource.password | 大小写+数字+特殊字符≥16位 | 不使用默认密码 |
| **Redis密码** | spring.redis.password | 复杂密码 | 禁止无密码运行 |
| **防火墙** | iptables/firewalld | 仅开放80/443/22端口 | 关闭其他不必要端口 |
| **文件上传** | ruoyi.profile | 限制文件类型和大小 | 防止上传恶意文件 |
| **SQL注入** | MyBatis参数化查询 | 使用#{param}而非${param} | 已由框架保证 |
| **XSS防护** | 过滤特殊字符 | 前后端双重过滤 | 防止跨站脚本攻击 |
| **CSRF防护** | Token机制 | JWT本身防CSRF | 无状态Token天然免疫 |
| **日志审计** | 操作日志 | @Log注解记录关键操作 | 满足合规要求 |

---

### 11.6 备份与恢复策略

#### 数据库备份（每日自动）

```bash
#!/bin/bash
# backup_mysql.sh - 数据库备份脚本

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR=/data/textbook/backup
MYSQL_USER=root
MYSQL_PASSWORD=your_password
DB_NAME=ry-vue

# 创建备份目录
mkdir -p $BACKUP_DIR

# 全量备份（mysqldump）
mysqldump -u$MYSQL_USER -p$MYSQL_PASSWORD \
  --single-transaction \
  --routines \
  --triggers \
  $DB_NAME | gzip > $BACKUP_DIR/full_$DATE.sql.gz

# 保留最近30天备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

echo "[$DATE] Backup completed: full_$DATE.sql.gz"
```

**定时任务** (crontab):
```bash
# 每天凌晨2点执行全量备份
0 2 * * * /data/textbook/scripts/backup_mysql.sh >> /var/log/backup.log 2>&1
```

#### 恢复流程

```bash
# 1. 停止应用
systemctl stop textbook-app

# 2. 恢复数据库
gunzip < /data/textbook/backup/full_20260416_020000.sql.gz | mysql -u root -p ry-vue

# 3. 验证数据
mysql -u root -p -e "SELECT COUNT(*) FROM textbook_book;" ry-vue

# 4. 重启应用
systemctl start textbook-app

# 5. 检查日志
tail -f /data/textbook/logs/sys-info.log
```

---

## 附录

### A. 项目目录结构（关键文件索引）

```
ruoyi-Vue/
├── ruoyi-system/src/main/java/com/ruoyi/textbook/
│   ├── controller/
│   │   ├── TbBookController.java              # 教材管理
│   │   ├── TbBuyController.java               # 购书管理 ⚡️ Fix #9/#12
│   │   ├── TbPurchaseController.java          # 购书单管理 ⚡️ Fix #9
│   │   ├── TbInventoryController.java         # 库存管理 ⚡️ Fix #1
│   │   ├── TbInboundController.java           # 入库管理
│   │   ├── TbOutboundController.java          # 出库管理
│   │   ├── TbShortageController.java          # 缺货管理 ⚡️ Fix #14
│   │   ├── TbSupplierController.java          # 供应商管理 ⚡️ Fix #11
│   │   └── TbNoticeController.java            # 通知管理 🆕
│   │
│   ├── service/
│   │   ├── ITbBuyService.java                 # 购书服务接口
│   │   ├── impl/TbBuyServiceImpl.java         # 购书服务实现 ⚡️ Fix #8
│   │   ├── ITbShortageService.java            # 缺书服务接口 ⚡️ Fix #14
│   │   ├── impl/TbShortageServiceImpl.java    # 缺书服务实现 ⚡️ Fix #14
│   │   ├── ITbInboundService.java             # 入库服务接口
│   │   ├── impl/TbInboundServiceImpl.java     # 入库服务实现 ⚡️ 流水+缺书联动
│   │   ├── ITbOutboundService.java            # 出库服务接口
│   │   ├── impl/TbOutboundServiceImpl.java    # 出库服务实现 ⚡️ 乐观锁+流水
│   │   ├── NoticeService.java                 # 通知服务 🆕
│   │   ├── ITbStockLogService.java            # 流水日志服务 🆕
│   │   └── impl/TbStockLogServiceImpl.java    # 流水日志实现 🆕
│   │
│   ├── domain/
│   │   ├── dto/
│   │   │   └── TbPurchaseImportDTO.java       # Excel导入DTO ⚡️ 固定列
│   │   ├── TbPurchase.java                    # 购书实体 ⚡️ +fileHash
│   │   ├── TbShortage.java                    # 缺书实体
│   │   ├── TbSupplier.java                    # 供应商实体 ⚡️ +物流字段
│   │   ├── TbStockLog.java                    # 流水日志实体 🆕
│   │   └── SysNotice.java                     # 通知实体(扩展) 🆕
│   │
│   └── mapper/
│       ├── TbPurchaseMapper.java              # 购书Mapper ⚡️ +selectByFileHash
│       └── TbStockLogMapper.java              # 流水日志Mapper 🆕
│
├── ruoyi-system/src/main/resources/mapper/textbook/
│   ├── TbPurchaseMapper.xml                   # 购书SQL ⚡️ +file_hash
│   ├── TbInventoryMapper.xml                  # 库存SQL ⚡️ +乐观锁更新
│   └── TbStockLogMapper.xml                   # 流水日志SQL 🆕
│
├── ruoyi-ui/src/views/textbook/
│   ├── purchase/index.vue                     # 采购页面 ⚡️ 导入对话框
│   └── notice/index.vue                       # 通知页面 🆕
│
└── sql/
    ├── upgrade_textbook_v2_2_dict.sql         # 字典初始化
    ├── upgrade_textbook_v3_final.sql          # v3升级脚本
    ├── upgrade_textbook_v3_compat.sql         # 兼容版升级
    └── upgrade_textbook_v3_fix.sql            # 最终修复版
```

### B. 数据库变更汇总 (v3.0)

| 表名 | 变更类型 | 变更内容 | 版本 |
|------|:--------:|---------|:----:|
| sys_notice | ALTER | +target_user_id, user_type, biz_id, biz_type, read_status | v2.0→v3.0 |
| textbook_buy | ALTER | +funding_source, **+file_hash** | v2.0→v3.0 |
| tb_supplier | ALTER | +user_id, logistics_company, logistics_no, ship_time, ship_status | v2.0→v3.0 |
| tb_stock_log | CREATE | 新建库存流水日志表 | v3.0 |
| sys_dict_data | INSERT | 12个学院 + 20个专业字典数据 | v2.0→v3.0 |

### C. 关键代码片段索引

| 功能 | 文件 | 方法/行号 |
|------|------|-----------|
| 库存编辑保护 | TbInventoryController.java | `edit()` |
| 删除保护(TbBuy) | TbBuyController.java | `delete()` L79-L89 |
| 删除保护(TbPurchase) | TbPurchaseController.java | `remove()` L92-L102 |
| 教师数据隔离 | TbBuyController.java | `list()` L42-L53 |
| MD5计算 | TbBuyServiceImpl.java | `calculateFileMD5()` L548-L562 |
| MD5查重 | TbBuyServiceImpl.java | `importFromExcel()` 内部调用 |
| selectByFileHash | TbPurchaseMapper.java | L86-L87 |
| selectByFileHash SQL | TbPurchaseMapper.xml | L143-L146 |
| ISBN聚合转采购 | TbShortageServiceImpl.java | `batchConvertToPurchase()` L176-L295 |
| 聚合Controller入口 | TbShortageController.java | `convertToPurchase()` L115-L127 |
| 入库处理(增强) | TbInboundServiceImpl.java | `processInbound()` |
| 出库处理(增强) | TbOutboundServiceImpl.java | `processOutbound()` |
| 供应商发货确认 | TbSupplierController.java | `confirmShip()` |
| 供应商数据隔离 | TbSupplierController.java | `myPurchaseList()` |
| 乐观锁更新 | TbInventoryMapper.xml | `updateInventoryQuantityWithCheck()` |

---

## 文档修订记录

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|---------|------|
| **v4.0** | **2026-04-16** | **重大新增**：领书通知/领书单完整模块（3张新表+18个后端文件+6个前端文件），打印功能、分批出库、数据字典，100%覆盖设计文档要求 | AI Assistant |
| **v3.0** | **2026-04-15** | **重大更新**：14项安全审计(#1/#8/#9/#12/#14)，新增通知系统、库存流水、Excel导入专项设计、ISBN聚合设计、安全分层架构、ER图更新、数据库变更汇总 | AI Assistant |
| v2.0 | 2026-04-15 | 角色体系重构v4.0，三角色模型(教师/库管员/供应商)，完整业务流程图 | AI Assistant |
| v1.0 | 2026-04-13 | 初稿，基础架构设计、数据库设计、核心流程 | AI Assistant |

---

*本文档为 RuoYi-Vue 教材管理系统的核心技术文档*
*API详情请参考[《教材管理系统 API 文档.md`](./教材管理系统%20API%20文档.md)*
*测试指南请参考[《教材管理系统-测试账号手册.md`](./教材管理系统-测试账号手册.md)*
*业务流程详细说明请参考[《教材采购系统_完整流程设计.md`](./教材采购系统_完整流程设计.md)*
